# Gradle
.gradle/
build/
app/build/

# 本机配置（含 SDK 路径，不同机器不同）
local.properties

# IDE
.idea/
*.iml
.vscode/

# 系统
.DS_Store

# 签名密钥
# 仓库公开后不再内置签名密钥（防伪造 APK / 签名劫持）：
# CI 每次构建用 keytool 临时生成同名调试密钥（见 build-apk.yml），
# 本地如有需要请自行生成到 keystore/focusguard-debug.jks。
keystore/
