# Gradle
.gradle/
build/
app/build/

# 本机配置（含 SDK 路径，不同机器不同）
local.properties

# IDE
.idea/
*.iml
.vscode/

# 系统
.DS_Store

# 签名密钥
# 仓库公开后不内置签名密钥；CI 从 GitHub Actions Secrets 恢复长期证书。
# 本机长期证书保存在仓库外的受限目录，keystore/ 始终不得提交。
keystore/
