# FocusGuard 设计规格 — 纸墨时间 · 琥珀夜光

> 品牌侧规格：约束 FocusGuard 全部界面（锁机页、悬浮窗、答题/朋友验证、设置页）。
> 改版目标：去除 Material 3 模板紫的「AI 味」，建立「时间账本 / 夜光钟表」的视觉人格。

## 1. Objective

- 产品：FocusGuard —— 自律锁机与专注计时（Android，Kotlin + Compose，辅以传统 View 悬浮窗）。
- 90% 状态：用户在锁机倒计时页面停留。这是品牌的第一现场，也是视觉投入的主战场。
- 设计目标：界面看起来像一块安静的夜光钟表，而不是科幻面板；克制的墨色 + 单一琥珀强调，任何一屏只有一处视觉焦点。

## 2. Product Context

- 用户是自我管理者：需要的是「可信、安静、不被装饰打扰」的锁机体验。
- 双渲染栈：Compose（Activity 界面）与传统 View（悬浮窗）。两栈必须共用同一令牌表，否则悬浮窗与锁机页切换时出现「两套皮肤」的感知断裂。
- 交互受限场景多（锁机中不可退出），可读性与层级优先于装饰。

## 3. Visual Foundations

### 3.1 Color — 墨纸令牌（深色默认）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--ink` | `#0E1217` | 背景最深（墨），全屏沉浸页 |
| `--paper` | `#151B22` | 主面板/二级背景 |
| `--card` | `#1B232C` | 卡片/输入底 |
| `--line` | `#2A343F` | 边框与分隔线 |
| `--amber` | `#E2A65D` | **唯一强调色**（夜光琥珀）：主按钮、进度、焦点、大数字点缀 |
| `--amber-deep` | `#B07E42` | 琥珀按压态 / 次级强调 |
| `--paper-text` | `#EDE6D6` | 主文字（暖纸白，非冷白） |
| `--haze` | `#8F887A` | 次级文字（暖灰） |
| `--faint` | `#5B574E` | 三级文字/占位 |
| `--brick` | `#C9776A` | 错误（砖红，暖调） |
| `--sage` | `#8AAE8C` | 成功（灰绿，暖调） |

- 禁用色：`#7C4DFF`/`#D0BCFF`/`#4F378B` 等 M3 模板紫全量退役；`#4CAF50`/`#F44336`/`#FF9800` 默认语义色退役。
- 渐变纪律：禁止多色渐变。唯一允许 = 锁机页中央 `--amber` 径向光晕（透明度 ≤ 6%），模拟夜光表盘反光；其余全部纯色。
- 透明度层级（代替纯白透明卡片的层次法）：边框 `--line` 100%；卡片内高亮面 `#FFFFFF` 3%；纯白 4% 大圆角填充退役。

### 3.2 Color — 主题变体（设置内 4 套 = 三个候选方向 + 浅色）

| 模式 | 存储值 | 背景 | 面板 | 卡片 | 边框 | 文字 | 强调色 | 对应方向 |
|---|---|---|---|---|---|---|---|---|
| 深色·墨（默认，原「深色·紫」） | 0 | `#0E1217` | `#151B22` | `#1B232C` | `#2A343F` | `#EDE6D6` | 琥珀 `#E2A65D` | 纸墨时间·琥珀夜光（已选） |
| 深色·简（原「深色·蓝」） | 1 | `#050607` | `#0C0E10` | `#121518` | `#23272B` | `#F2F2F0` | 信号红 `#E5484D` | 极简计时·纯黑白信号 |
| 深色·苔（原「深色·绿」） | 2 | `#0D1411` | `#131B17` | `#18211C` | `#263029` | `#E4E9E2` | 鼠尾草 `#9BB894`（次杏 `#C9A87C`） | 苔原专注·鼠尾草绿 |
| 浅色·纸 | 3 | `#F4F0E6` | `#EDE7D8` | `#E5DECD` | `#D6CDB9` | `#1D1A14` | 深琥珀 `#A9742F` | 浅色补充 |

- 每个方向有**独立强调色**（琥珀/信号红/鼠尾草），错误/成功色随变体微调但保持暖调家族。
- 存储值不变：用户已保存的主题选择无缝迁移，仅标签与色板更换。
- 锁机页与悬浮窗**跟随所选主题**；浅色主题下锁机页/悬浮窗回退「深色·墨」（夜光表盘需暗场）。

### 3.3 Type

| 角色 | 字族 | 规格 | 用途 |
|---|---|---|---|
| Display 数字 | `FontFamily.Serif`（系统衬线） | Bold, 40–56sp, 字距 +0.05em | 倒计时大数字（签名元素，钟表刻度质感） |
| 标题 | `FontFamily.SansSerif` | Medium/Bold, 16–24sp | 中文标题 |
| 正文 | `FontFamily.SansSerif` | 14–16sp | 说明文字 |
| Eyebrow 标签 | `FontFamily.SansSerif` | 10–11sp, 字距 +0.4em, `--haze` | 顶部状态标签（阶段/锁机原因/徽章） |
| 数据/密文 | `FontFamily.Monospace` | 15–17sp | 朋友密码密文、偏移量 |

- 衬线数字在部分中文 ROM 上由系统 Noto Serif 兜底，阿拉伯数字渲染稳定；若异常则回退 SansSerif Bold（见实现注释）。

### 3.4 Shape / Elevation / Spacing

- 圆角收敛：卡片 12dp，按钮/FilterChip 10–12dp（统一族），小徽章 8dp，进度环保持圆形；20dp 大圆角退役。
- 层次：细边框（1dp `--line`）代替投影；悬浮窗/锁机页不使用 Material 阴影。
- 间距节奏：4dp 基数，区块间距 24dp，卡片内边距 16dp。
- 触控目标 ≥ 44dp（键盘按键、按钮不变）。

### 3.5 Motion — 夜光钟表语汇

原则：**一个编排时刻 > 散落动效**；所有装饰动画受系统「动画时长缩放」约束（缩放为 0 时跳过）。

- **锁机页进入编排**（唯一大编排）：eyebrow 标签先淡入（160ms）→ 衬线倒计时 fade+scale 落下（240ms, spring 阻尼）→ 操作区自底部上移淡入（200ms）。之后不再有整页动画。
- **倒计时数字**：秒位变化用 120ms crossfade；分钟位变化用轻 scale 弹跳（1.0→1.04→1.0）。不旋转、不滚动、不发光闪烁。
- **琥珀光晕**：单色径向光晕 alpha 4%→7% 呼吸，周期 5s，仅锁机页中央。
- **进度环**：剩余时间变化时弧线平滑收缩（250ms tween），不循环旋转。
- **悬浮窗界面切换**（传统 View，ViewPropertyAnimator ≤200ms）：锁机主页 ↔ 答题/朋友验证用淡入淡出 + 12dp 上移；键盘按键按下 scale 0.94 回弹；提交失败反馈区 shake（translationX ±6dp × 3）。
- **设置页**：不加装饰动画，仅默认涟漪。
- **守卫**：动画时长全部 ≤250ms；悬浮窗动画只作用于子 View，不触碰窗口层级（防守护 tick 卡顿）；Compose 侧用 `LocalView` 检查 animator scale，View 侧复用同一检查。

## 4. Accessibility

- 文字对比：`--paper-text` 对 `--ink` ≥ 12:1；`--haze` ≥ 4.5:1；`--faint` 仅用于非关键信息。
- 琥珀主按钮上的文字用 `--ink` 深色（对比 ≥ 7:1），不用白字。
- 错误信息不止靠颜色：保留文字说明（现有反馈区模式）。
- 动效克制：仅倒计时跳动与光晕呼吸，且受系统「减弱动画」设置约束。

## 5. Voice & Tone

- 中文、直接、克制。动作按钮说清结果（「验证密码」「提交答案」），不说空话。
- 时间表达：大数字 + 最小单位标签（时/分/秒），不写「还差一点点」类情绪文案。
- 箴言保留为浅色小字（`--haze` 12sp），是产品功能而非装饰。

## 6. Implementation Practices

- 新建 `ui/theme/FocusColors.kt`：Compose `Color` 常量 + 传统 View 用十六进制字符串常量（同一令牌双栈共享）。
- `Theme.kt` 的 4 套 `darkColorScheme/lightColorScheme` 重映射到本表；`ThemeModes.labelOf` 文案更新（「深色·紫」→「深色·墨」），模式 int 值不变（设置存储兼容）。
- 页面内硬编码 `Color(0xFF...)` 一律替换为令牌引用；悬浮窗 `Color.parseColor("#...")` 替换为字符串常量。
- 锁机页背景：`--ink` 纯色 + 单色径向光晕；移除 `Brush.verticalGradient` 紫渐变。
- 悬浮窗传统 View 与 Compose 锁机页必须逐屏对照同一令牌，不允许任何一屏出现旧紫。

## 7. Anti-Patterns

- 禁用：M3 模板紫家族；高饱和多色渐变；白色 4% 大圆角卡片；每屏多个实底强调按钮；emoji 装饰；无功能意义的渐变分割线；循环旋转的装饰动画；超过 250ms 的界面切换动画。
- 每屏最多一个琥珀实底主按钮（主操作）；其余用细边框幽灵按钮。
- 锁机页不允许出现第二强调色（状态色砖红/灰绿除外）。

## 8. Decision-Making

- 强调色永远优先从 `--amber` 取；新语义色需先过「暖调 + 与墨底 ≥4.5:1」两道关。
- 设置页走 Convention 原则：只换令牌与文案，不改布局结构。
- 任何页面若要在墨底上引入第二个色相，必须给出一条 Decision Trace 理由。

## 9. Workflow

- 改版顺序：令牌 → 主题 → 锁机页 Compose → 悬浮窗 View → 其余 Activity → 设置页 → 动效层 → CI + 复审。
- 每完成一块，用 product-design 的 QA rubric 做静态核对（颜色均来自令牌表、无旧紫残留、无新增渐变）。
- 变更记录到 `AGENTS.md`；本文件是唯一视觉真相源，后续会话（Move 0）先读它再动手。
